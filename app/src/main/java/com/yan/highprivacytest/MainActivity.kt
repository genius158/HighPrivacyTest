package com.yan.highprivacytestimport android.content.Intentimport android.content.IntentFilterimport android.os.Bundleimport android.view.Viewimport androidx.appcompat.app.AppCompatActivityimport com.yan.highprivacy.PrivacyMgrclass MainActivity : AppCompatActivity() {    override fun onCreate(savedInstanceState: Bundle?) {        super.onCreate(savedInstanceState)        setContentView(R.layout.activity_main)        findViewById<View>(R.id.tvTest).setOnClickListener {            PrivacyMgr.privacy.dispatchUnAuth()        }//        startService(Intent(this, TestService::class.java))        val intentFilter = IntentFilter()        intentFilter.addAction("android.net.conn.CONNECTIVITY_CHANGE")        registerReceiver(TestBroadCastReceiver2(), intentFilter)        val it = Intent(Intent.ACTION_EDIT)        it.putExtra("msg", "boardreceiver test2222")        sendBroadcast(it)    }}